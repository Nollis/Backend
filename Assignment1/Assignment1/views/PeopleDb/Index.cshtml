@model List<Person>
@{
    ViewData["Title"] = "Index";
}

<div><a asp-action="Create" class="btn">Create a new person</a></div>

<h1>List of people</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Phone</th>
            <th>City</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var person in Model) 
        {
            <tr>
                <td>@person.PersonId</td>
                <td>@person.Name</td>
                <td>@person.Phone</td>
                    <td>@person.City</td>
                <td><a asp-action="Delete" asp-route-id="@person.PersonId class="btn">Delete</a></td>
                <td><a asp-controller="PersonCityDb" asp-action="Index" class="btn" asp-route-id=@person.PersonId>City connection</a></td>
            </tr>
        }
    </tbody>
</table>
