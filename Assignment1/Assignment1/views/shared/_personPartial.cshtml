@model PersonModel

<div class="row">
    <div class="col">@Model.Id</div>
    <div class="col">@Model.Name</div>
    <div class="col">@Model.Phone</div>
    <div class="col">@Model.City</div>
    <div class="col"><a asp-action="Delete" asp-controller="People" asp-route-id=@Model.Id>Delete</a></div>
</div>