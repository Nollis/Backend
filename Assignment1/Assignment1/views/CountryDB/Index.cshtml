@model List<Country>
@{
    ViewData["Title"] = "Index";
}

<h1>List of Countries</h1>

<div><a asp-action="Create" class=" btn">Create a new Country</a></div>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var country in Model)
        {
                <tr>
                    <td>@country.Id</td>
                    <td>@country.Name</td>
                    <td><a asp-action="Delete" asp-route-id="@country.Id">Delete</a></td>
                </tr>
        }
    </tbody>
</table>